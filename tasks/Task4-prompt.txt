Task 3 (Replication + Fault Tolerance) is completed and already integrated into the project.

Important Notes Before Starting Task 4:

1. Replication System Already Exists

* The master replication package is implemented inside:
  master/replication/
* Do NOT rewrite the replication flow unless necessary.

2. All Writes Must Go Through Master

* Slaves should NEVER expose public write APIs.
* Insert/update/delete/create-table operations should happen only through the master node.
* The master automatically replicates successful operations to slaves.

3. Existing Replication Endpoint
   Already implemented:
   POST /replicate

Current responsibilities:

* receive replication request
* identify operation type
* call:

  * handleInsert()
  * handleUpdate()
  * handleDelete()

Your job is to replace placeholder logic with real DB queries.

4. Do NOT Duplicate SQL Logic
   Use the shared database/query layer created by Member 2.
   Avoid writing duplicate SQL execution code inside each slave.

5. Read APIs Are Your Main Responsibility
   You should now implement:

* GET /select
* GET /search

These endpoints should:

* fetch replicated data
* return JSON responses
* mainly serve read operations

6. Retry + Fault Tolerance Already Handled
   The master replication package already:

* retries failed requests
* handles offline slaves
* logs failures/success
* keeps the system alive if one node fails

You do not need to implement retry logic again.

7. Health Endpoint Already Exists
   Available on:
   GET /health

8. Replication Request Format
   Current request structure:

type ReplicationRequest struct {
Operation string
Database  string
Table     string
Data      interface{}
}

9. Cross-Technology Worker Node
   For the bonus:

* create an additional worker node using:

  * Python Flask
    OR
  * Node.js Express

The worker should:

* receive requests from master
* process data
* return response

10. Recommended Priority Order

11. Connect slaves to real database

12. Implement real insert/update/delete queries

13. Implement select/search APIs

14. Add logging

15. Implement bonus worker node

16. Perform integration testing with master

17. Important Architecture Rule
    Slaves are replica/read nodes.
    They should mainly:

* receive replicated data
* answer read queries

Master remains the only write coordinator.
